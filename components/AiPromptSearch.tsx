const defaultPrompts = [
    "Weekend escape with hidden cafés and local markets",
    "Beachside sunrise trip with scenic photo spots",
    "City culture tour with museums and rooftop dining",
    "Adventure weekend with hikes and cozy stays",
];

const AiPromptSearch = () => {
  return (
    <section>
        AI SEARCH
    </section>
  )
}

export default AiPromptSearch
